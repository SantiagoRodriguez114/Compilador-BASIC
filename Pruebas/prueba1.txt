' PRUEBA GENERAL — ANALIZADOR SINTACTICO

' 1. DECLARACIONES (Una de cada tipo de dato básico)
DIM contador AS INTEGER
DIM precio AS DOUBLE
DIM nombre AS STRING
DIM activo AS BOOLEAN
CONST PI = 3.1416

' 2. ASIGNACIONES Y EXPRESIONES
contador = (5 + 3) * 2
precio = 15.75 ^ 2
activo = TRUE AND NOT FALSE
nombre = "Santiago" & " Rodriguez"
LET contador = 20

' 3. ESTRUCTURAS CONDICIONALES
IF contador > 5 THEN
    PRINT contador
END IF

IF contador > 100 THEN
    PRINT "Muy grande"
ELSEIF contador > 50 THEN
    PRINT "Grande"
ELSE
    PRINT "Pequeño"
END IF

' 4. BUCLES Y CICLOS
WHILE contador < 20
    contador = contador + 1
WEND

DO
    contador = contador + 1
LOOP UNTIL contador = 30

DO WHILE contador < 40
    contador = contador + 1
LOOP

FOR i = 1 TO 10
    PRINT i
NEXT

FOR j = 1 TO 20 STEP 2
    PRINT j
NEXT j

' 5. ANIDAMIENTO (Ciclo que contiene un condicional)
FOR k = 1 TO 3
    IF k > 1 THEN
        PRINT k
    END IF
NEXT

' 6. SUBRUTINAS Y FUNCIONES (Declaración y Llamados)
SUB sumar(a AS INTEGER, b AS INTEGER)
    PRINT a
    PRINT b
END SUB

FUNCTION calcular
    PRINT "Calculando"
END FUNCTION

CALL sumar(5, 10)

' 7. INTERACCIÓN Y CONTROL
INPUT "Ingrese edad"; contador
RETURN
EXIT FOR

' =====================================================================
' 8. SECCIÓN DE ERRORES SINTÁCTICOS CONTROLADOS
' =====================================================================

DIM AS INTEGER
10 = contador
FOR x = 1 10
PRINT "Fin de la prueba"
